functions == filters
error handling
shell vs command
ansible to create ec2 instances and r53 records

ansible controller/server == where ansible installed

server ni connect ayye process or ways
===================
console  ---> server ni connect ayyamu 
aws cli ---server ni connect ayyamu
ansible  ---> server ni connect ayyamu 
terraform
python

ansible-roboshop folder lo
1. mongodb.yaml file done and mongo.repo also done
after login mongodb server u can use this cmd like netstat -lntp 

2 . redis-yaml file done ---> ansible.builtin.lineinfile

3. musql-yaml file has been done
4. RabbitMQ-yaml file done
5. cata-yaml

# so intha varaku runcheste manaki error avstundhi like --The error was: If you are using a module and expect the file to exist on the remote, see the remote_src option
# so here remote server lo kadha task lu excute ayyedhi bcz we are configuring the remoteserver kabbati.. not in ansible server...  
# by default ansible server lo numchi file ni remote serevr loki move chayali feel avuthudni, but actuall ga remote lone file vuntadi download ayinapdu... we have a one option like as below remote_src

  when: catalogue_output.stdout | int < 0 here stdout value lo 0 kate thakuva ithe manam data ni load checi ani cheptunam for more u can see session once
int --> ansible string to int https://serverfault.com/questions/1030350/converting-string-to-integer-in-ansible-playbook